# @package _global_

# Overrides for the 'train_memory' portion of the 'base' experiment.
# This file will be loaded when running:
# python -m src.training.train_memory experiment=base

# Example: Override specific task parameters for train_memory
task:
  sample_n: 777 # Use a specific sample size for this base experiment's memory run
  learning_rate: 5e-4 # Specific learning rate for this base experiment's memory run
