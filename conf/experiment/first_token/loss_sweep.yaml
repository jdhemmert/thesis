# @package _global_

defaults:
    - first_token/common
    - _self_


experiment_subdir: "loss_sweep"


model:
    model_config:
        model_path: "meta-llama/Llama-3.2-3B"


hydra:
    mode: MULTIRUN
    sweep:
        subdir: ${task.alpha}alpha_${task.temperature}temp
    sweeper:
        params:
            task.alpha: 0.0, 0.25, 0.5, 0.75, 1.0
            task.temperature: 0.1, 0.25, 0.5, 0.75, 1, 1.25, 1.5, 1.75, 2.0