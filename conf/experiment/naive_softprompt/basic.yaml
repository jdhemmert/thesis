# @package _global_

defaults:
  - override /task:  train_memory
  - override /model/model_config: augmented_llama
  - _self_

task:
    trainable_strategy: soft_prompt_only
    extrinsic_validation: end
    save_strategy: "no"
